<h1>Eat Da Burger!!!</h1>

<h2>Eat All Of The Burgers!!!</h2>

<ul>
  {{#each burger_name}}
    {{#unless devoured}}
      {{> burger_name/burger_name-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Try Out This Burger!!!</h2>

<ul>
  {{#each burger_name}}
    {{#if devoured}}
      {{> burger_name/burger_name-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add Your Favorite Burger!</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="slee">Devoured?</label><br>
    <input type="radio" name="devoured" value="1" checked> Eaten!<br>
    <input type="radio" name="devoured" value="0"> Not Eaten
  </div>

  <button type="submit">Make A Burger!</button>
</form>